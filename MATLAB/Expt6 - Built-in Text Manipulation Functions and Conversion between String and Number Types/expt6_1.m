% Text Manipulation Script
inputStr = input('Enter a string: ', 's');
upperStr = upper(inputStr);
lowerStr = lower(inputStr);
fprintf('Uppercase: %s\n', upperStr);
fprintf('Lowercase: %s\n', lowerStr)

