ASSUME CS: CODE, DS: DATA
DATA SEGMENT
	M1 DB 10,13,"ENTER FIRST NUMBER $"
	M2 DB 10,13,"ENTER SECOND NUMBER $"
	M3 DB 10,13,"SUM IS  $"
DATA ENDS

CODE SEGMENT

START: 
	MOV AX,DATA
	MOV DS,AX
	LEA DX,M1
	MOV AH,09H
	INT 21H
	MOV AH,01H
	INT 21H
	SUB AL,30H
	MOV BL,AL
	LEA DX,M2
	MOV AH,09H
	INT 21H
	MOV AH,01H
	INT 21H
	SUB AL,30H
	MOV CL,AL
	ADD BL,CL
	LEA DX,M3
	MOV AH,09H
	INT 21H

	MOV DH,0AH
	MOV AH,00H
	MOV AL,BL
	DIV DH
	
    MOV CL,AH
	ADD AL,30H
	MOV DL,AL
	
    MOV AL,02H
	INT 21H
	
    MOV AL,CL
	ADD AL,30H
	MOV DL,AL
	
    MOV AH,02H
	INT 21H

	MOV AH,4CH
	INT 21H
CODE ENDS
END START
